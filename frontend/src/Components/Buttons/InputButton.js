const InputButton = ({ text }) => {
    return (
        <button
            className="input-button"
            type='button'>
            {text}
        </button>
    )
}

export default InputButton